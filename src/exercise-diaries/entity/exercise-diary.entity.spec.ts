import { ExerciseDiaryEntity } from './exercise-diary.entity';

describe('ExerciseDiaryEntity', () => {
  it('should be defined', () => {
    expect(new ExerciseDiaryEntity()).toBeDefined();
  });
});
